{
  "name": "juke",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:smoke": "playwright test tests/smoke",
    "test:auth": "playwright test tests/smoke/auth-gate.spec.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.45.0",
    "serve": "^14.2.3"
  }
}
